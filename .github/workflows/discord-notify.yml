name: 🛰 Discord Notifications Pro

on:
  push:
    branches:
      - '**'
  pull_request:
    types: [opened, closed, merged]
  issues:
    types: [opened, closed, edited, assigned, unlabeled]

jobs:
  notify:
    runs-on: ubuntu-latest

    steps:
      - name: ✅ Checkout repository
        uses: actions/checkout@v4
        continue-on-error: true

      - name: 🛰 Prepare Discord Message
        id: prepare
        shell: bash
        run: |
          set -e
          echo "GitHub event: ${{ github.event_name }}"
          echo "Branch: ${{ github.ref_name }}"

          COLOR=15105570
          if [[ "${{ github.ref_name }}" == "main" ]]; then COLOR=3066993; fi
          if [[ "${{ github.ref_name }}" =~ ^(dev|develop)$ ]]; then COLOR=3447003; fi
          echo "color=$COLOR" >> $GITHUB_OUTPUT

          PAYLOAD_FILE="payload.json"

          if [[ "${{ github.event_name }}" == "push" ]]; then
cat <<'EOF' > $PAYLOAD_FILE
{
  "embeds": [{
    "title": "📦 Новый Push",
    "color": COLOR_PLACEHOLDER,
    "fields": [
      { "name": "Репозиторий", "value": "${{ github.repository }}", "inline": true },
      { "name": "Ветка", "value": "${{ github.ref_name }}", "inline": true },
      { "name": "Автор", "value": "${{ github.actor }}", "inline": true },
      { "name": "Коммит", "value": "${{ github.event.head_commit.message || 'N/A' }}" }
    ],
    "url": "${{ github.event.head_commit.url || github.server_url }}",
    "footer": { "text": "GitHub Actions • Push Event" }
  }]
}
EOF
            sed -i "s/COLOR_PLACEHOLDER/$COLOR/" $PAYLOAD_FILE
          elif [[ "${{ github.event_name }}" == "pull_request" ]]; then
cat <<'EOF' > $PAYLOAD_FILE
{
  "embeds": [{
    "title": "💬 Pull Request ${{ github.event.action }}",
    "color": COLOR_PLACEHOLDER,
    "fields": [
      { "name": "PR", "value": "[#${{ github.event.pull_request.number }}](${{ github.event.pull_request.html_url }})", "inline": true },
      { "name": "Автор", "value": "${{ github.actor }}", "inline": true },
      { "name": "Название", "value": "${{ github.event.pull_request.title }}" }
    ]
  }]
}
EOF
            sed -i "s/COLOR_PLACEHOLDER/$COLOR/" $PAYLOAD_FILE
          elif [[ "${{ github.event_name }}" == "issues" ]]; then
cat <<'EOF' > $PAYLOAD_FILE
{
  "embeds": [{
    "title": "📝 Issue ${{ github.event.action }}",
    "color": COLOR_PLACEHOLDER,
    "fields": [
      { "name": "Issue", "value": "[#${{ github.event.issue.number }}](${{ github.event.issue.html_url }})", "inline": true },
      { "name": "Автор", "value": "${{ github.actor }}", "inline": true },
      { "name": "Название", "value": "${{ github.event.issue.title }}" }
    ]
  }]
}
EOF
            sed -i "s/COLOR_PLACEHOLDER/$COLOR/" $PAYLOAD_FILE
          fi

      - name: 💌 Send to Discord
        run: |
          if [ -f payload.json ]; then
            curl -H "Content-Type: application/json" \
                 -X POST \
                 -d @payload.json \
                 "${{ secrets.DISCORD_WEBHOOK_URL }}"
          else
            echo "⚠️ No payload.json found — skipping Discord notification."
          fi
